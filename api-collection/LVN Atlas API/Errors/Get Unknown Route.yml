info:
  name: Get Unknown Route
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/does-not-exist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
