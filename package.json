{
  "name": "lvn-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "A backend-first relocation decision platform built using production-grade software engineering practices.",
  "license": "MIT",
  "packageManager": "pnpm@11.13.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @lvn/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "format": "pnpm -r format",
    "format:check": "pnpm -r format:check"
  }
}